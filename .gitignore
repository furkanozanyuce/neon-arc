# dependencies
node_modules

# build output
dist
dist-ssr

# env files (NEVER commit secrets — even though the anon key is safe, keep habits clean)
.env
.env.local
.env.*.local

# vercel
.vercel

# os / editor
.DS_Store
*.log
.vscode
.idea
*.swp
